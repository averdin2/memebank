/* test('on click makes call to localhost', () => {
  //https://i.imgur.com/KOoBDaK.gif is the image link we will use
  // This test is supposed to:
  // Open MemeBank's webpage
  // input that image link into FieldInputBank
  // click on the button
  //check that it returned the link
  // another test for hitting enter, everything will be the same except this line
  // once cards is working again, test that it returned that too
  expect();
});
*/
